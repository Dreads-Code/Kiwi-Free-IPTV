{
  "name": "iptv-nz-stremio-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npx -y wasm-pack build --target web --out-dir react-app/src/wasm -- --no-default-features && cargo build --release && npm install --prefix react-app && npm run build --prefix react-app",
    "vercel-build": "npm install --prefix react-app && npm run build --prefix react-app",
    "dev": "concurrently -n \"backend,frontend\" -c \"cyan,magenta\" \"cargo run --bin local\" \"npm run dev --prefix react-app\""
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
